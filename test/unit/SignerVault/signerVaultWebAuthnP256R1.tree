SignerVault__WebAuthnP256R1
├── it always store a signer to the same storage slots
├── it does not store two signers to the same storage slots
├── it should be possible to override existing signer
├── it return the stored signer given a client id
├── it return an empty signer if not found given a client id
├── it reverts if no signer found when using safe
├── it return true if signer exists given a client id hash
├── it return false if no signer exists given a client id hash
├── it return true if signer exists given a client id
├── it return false if no signer exists given a client id
├── it removes a stored signer based on a client id hash
├── it return the stored pubkey associated to a client id hash
├── it return true if verify correct webauthn payload
├── it return false if verify incorrect webauthn payload
└── it always use the same root
