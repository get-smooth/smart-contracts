SignerVault__VanillaP256K1
├── it always store a signer to the same storage slot
├── it does not store two signers to the same storage slots
├── it return true if signer exists given the owner address
├── it return false if no signer exists given the owner address
├── it remove a stored signer based on the owner address
└── it always use the same root
