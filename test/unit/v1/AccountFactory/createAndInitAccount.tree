AccountFactory__CreateAndInitAccount
├── it use a deterministic deployment process
├── it return existing account address given a hash already used
├── it deploy a new account if none exists given a new hash
├── it revert with an incorrect valid signature
├── it call initialize
├── it call the proxy addFirstSigner function
├── it trigger an event on deployment
└── it set the factory address in the proxy storage on init
